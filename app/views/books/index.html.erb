<h1>New book</h1>

<% @books.each do |book|%>

<P>Title</P>
<span>
    <%=link_to book.title, "/books/#{book.id}"%>
</span>
<P>Body</P>
<span>
    <%=link_to book.body, "/books/#{book.id}"%>
</span>

<% end %>